import React from 'react';
import { Button } from '@mui/material';

const Test = () => {
    return (
        <div>
            <Button variant="contained">Contained</Button>
        </div>
    );
}

export default Test;
